# تحليل مشروع School.ly (شرح واضح ومباشر)

## 1) المشروع هذا إيش يعمل؟
**School.ly** نظام إدارة مدرسة بسيط مبني بـ **Laravel + Inertia + React**.
فكرته الأساسية: المستخدم يسجل دخول، وبعدها يدير بيانات المدرسة (المعلمين، الطلاب، المواد، التسجيلات) مع فصل البيانات بين المدارس باستخدام `tenant_id`.

باختصار: كل مدرسة لها بياناتها، والداشبورد يعرض إحصائياتها.

---

## 2) المهام/الوحدات الأساسية في النظام
من خلال الـ Routes والواجهة:

1. **Dashboard**
   - يعرض: عدد الطلاب، المواد، المعلمين، المواد المميزة (Subjects)، وعدد التسجيلات.
   - يجيب اسم المدرسة من جدول `tenants`.

2. **Teachers (المعلمين)**
   - إضافة معلم
   - تعديل معلم
   - حذف معلم
   - عرض جميع معلمي نفس المدرسة

3. **Students (الطلاب)**
   - إضافة طالب
   - تعديل طالب
   - حذف طالب
   - عرض جميع طلاب نفس المدرسة

4. **Courses (المواد/الكورسات)**
   - إضافة مادة وربطها بمعلم (`teacher_id`)
   - تعديل/حذف مادة
   - عرض المواد الخاصة بنفس المدرسة

5. **Enrollments (التسجيلات)**
   - ربط طالب بمادة مع تاريخ تسجيل
   - تعديل/حذف التسجيل
   - عرض التسجيلات داخل نفس المدرسة

6. **Settings (الإعدادات)**
   - تحديث الملف الشخصي
   - تغيير كلمة المرور
   - تفعيل المصادقة الثنائية (2FA)
   - إعدادات المظهر

---

## 3) الصلاحيات (Permissions) في النظام
### المطبق فعليًا:
- أغلب صفحات النظام الأساسية داخل Middleware:
  - `auth` (لازم المستخدم مسجل دخول)
  - `verified` (لازم البريد الإلكتروني متحقق)
- إعدادات الحساب (`settings`) تتطلب `auth`.
- تغيير كلمة المرور عليه `throttle:6,1` (حماية من كثرة المحاولات).
- 2FA مفعلة عبر Fortify.

### غير الموجود حاليًا:
- **لا توجد Roles واضحة** (مثل admin/teacher/staff).
- **لا توجد Policies/Gates تفصيلية** للتحكم الدقيق لكل عملية CRUD حسب نوع المستخدم.

> النتيجة: أي مستخدم مسجل ومتحقق يقدر يوصل لكل وحدات CRUD (ضمن القيود الحالية للـ tenant).

---

## 4) الجداول (Database Tables)

## أ) جداول العمل الأساسية (Business)

### 1. `tenants`
- `tenant_id` (PK)
- `school_name`
- `address`
- `created_at`

### 2. `users`
- `id` (PK)
- `tenant_id` (nullable)
- `name`
- `email` (unique)
- `email_verified_at`
- `password`
- `remember_token`
- `created_at`, `updated_at`
- `two_factor_secret`
- `two_factor_recovery_codes`
- `two_factor_confirmed_at`

### 3. `teachers`
- `teacher_id` (PK)
- `tenant_id`
- `first_name`
- `last_name`
- `subject`

### 4. `students`
- `student_id` (PK)
- `tenant_id`
- `first_name`
- `last_name`
- `grade`

### 5. `courses`
- `course_id` (PK)
- `tenant_id`
- `course_name`
- `teacher_id`

### 6. `enrollments`
- `enrollment_id` (PK)
- `tenant_id`
- `student_id`
- `course_id`
- `enrollment_date`

## ب) جداول النظام (Laravel Infrastructure)
- `password_reset_tokens`
- `sessions`
- `cache`
- `cache_locks`
- `jobs`
- `job_batches`
- `failed_jobs`

---

## 5) العلاقات المنطقية بين الجداول
منطق النظام المتوقع:
- Tenant (مدرسة) واحدة ⟶ لديها Users / Teachers / Students / Courses / Enrollments.
- Teacher واحد ⟶ ممكن يرتبط بعدة Courses.
- Student + Course ⟶ يربطهم Enrollment.

**مهم:** العلاقات موجودة منطقيًا في الكود، لكن في المايغريشن الحالية ما في Foreign Keys صريحة تمنع الروابط الخاطئة على مستوى قاعدة البيانات.

---

## 6) كيف تمشي العملية داخل النظام؟
1. المستخدم يسجل/يدخل.
2. النظام يقرأ `tenant_id` من المستخدم الحالي.
3. كل Controller يفلتر البيانات بـ `tenant_id` عند العرض.
4. عند الإضافة، `tenant_id` يُحقن تلقائيًا من المستخدم الحالي.
5. عند التعديل/الحذف، يتم البحث داخل نفس `tenant_id` قبل تنفيذ العملية.

---

## 7) أخطر شيء في المشروع حاليًا
## **أخطر نقطة: ضعف العزل المرجعي بين جداول نفس الـ tenant (Cross-tenant integrity).**

ليش هذا أخطر شيء؟
- في إنشاء/تعديل `courses` و`enrollments`، التحقق غالبًا على `integer` فقط.
- ما في `exists` مقيّد بـ `tenant_id`.
- وما في Foreign Keys على مستوى DB.

هذا قد يسمح (خصوصًا عبر طلبات معدلة يدويًا) بربط سجل من مدرسة مع سجل من مدرسة ثانية إذا تم تمرير IDs صالحة رقميًا.

**التأثير:**
- خلط بيانات بين المدارس.
- تقليل موثوقية التقارير.
- احتمال تسرب سياقي للبيانات عبر روابط غير صحيحة.

---

## 8) ملاحظات سريعة
- المشروع ممتاز للتعلم والتجربة على CRUD + Inertia + React.
- بنية الوحدات واضحة وسهلة.
- يحتاج طبقة صلاحيات أدق + قيود قاعدة بيانات أقوى لو الهدف إنتاجي (Production).

---

## 9) مكان الملف
هذا التحليل محفوظ في:

`/home/runner/work/school.ly/school.ly/PROJECT_ANALYSIS_AR.md`

تقدر تنزله مباشرة من نفس المسار.
